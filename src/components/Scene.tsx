"use client";

/** Placeholder until the three.js scene lands in the next commit. */
export default function Scene({ className }: { className?: string }) {
  return <div className={className} data-scene />;
}
